{
  "name": "velle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:restart": "docker compose down && docker compose up -d --build",
    "docker:logs": "docker compose logs -f",
    "build": "cd backend && npm ci && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/src/main.js"
  },
  "description": "Velle - Plataforma Financiera Venezuela"
}
