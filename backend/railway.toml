# Railway - Velle Backend
# https://docs.railway.app/reference/config-as-code
# El Dockerfile define el CMD. No sobrescribir con startCommand.

[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
