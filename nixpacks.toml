# Nixpacks - deploy desde raíz
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd backend && npx prisma migrate deploy && node dist/src/main.js"
